/**
 * Styles are imported statically by `index.js` so a strict `style-src 'self'`
 * policy can remain enabled. Kept as a no-op for direct page-module imports.
 */
export function ensurePublicStyles() {}
